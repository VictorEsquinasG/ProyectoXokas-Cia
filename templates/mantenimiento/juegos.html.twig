{% extends 'base.html.twig' %}

{% block title %}JUEGOS 🎮⚙
{% endblock %}

{% block body %}

	<style>
		table {
			margin-top: 4.25rem;
		}
		span > svg {
			cursor: pointer;
		}
	</style>

	{# PINTAMOS LA TABLA Y LA RELLENAMOS #}
	<article style="width:90%;display:flex;justify-content:center;margin:auto">

		<table class="table table-striped table-primary">
			<thead class="thead-light">
				<tr>
					<th>ID</th>
					<th>Imagen</th>
					<th>Nombre</th>
					<th>Descripción</th>
					<th>Mínimo de Jugadores</th>
					<th>Máximo de Jugadores</th>
					<th>Ancho (tablero)</th>
					<th>Largo (tablero)</th>
					<th>
						<i class="fa fa-cog"></i>
					</th>
				</tr>
			</thead>
			<tbody>
				{# Listamos los juegos #}
				{% for juego in juegos %}
					<tr class="table-light">
						<td>{{ juego.getId() }}</td>
						<td> <img src="{{ asset('images/uploads/' ~ juego.getImagen()) }}" height="50" alt="imagen de {{ juego.getNombre() }}"></td>
						<td>{{ juego.getNombre() }}</td>
						<td>{{ juego.getDescripcion() }}</td>
						<td>{{ juego.getMinJugadores() }}</td>
						<td>{{ juego.getMaxJugadores() }}</td>
						<td>{{ juego.getAnchoTablero() }}</td>
						<td>{{ juego.getLargoTablero() }}</td>
						<td>
							<span>
								<a href="{{'/mantenimiento/juego/editar/' ~ juego.getId() }}">
									<i class="fa fa-pencil" title="editar"></i>
								</a>
							</span>
							<span>
								<a href="{{'/mantenimiento/juego/borrar/' ~ juego.getId() }}">
									<i class="fa fa-trash" title="borrar"></i>
								</a>
							</span>
						</td>
					</tr>
					<tr>
					{% endfor %}
				</tbody>
			</tbody>
			<tfoot>
				<tr>
					<th colspan="8" class="text-center">
						© Tabla cortesía del servidor
					</th>
				</tr>
			</tfoot>
		</table>
	</article>
{% endblock %}
