{% extends 'base.html.twig' %}

{% block title %}Catálogo
{% endblock %}

{% block body %}

    <h2 style="margin-left: 6%;">
        Estos son nuestros Juegos
        {# Si está logueado le llamamos por su nombre #}
        {% if app.user %}
            {{ app.user.getNombre }}
        {% endif %}
        !
    </h2>
    <h4 style="margin-left: 6%;">¿Qué te parece?</h4>

    {# CARDS DE JUEGOS #}
    {% include "components/cards-juegos.html.twig" %}
    
    {# EL MODAL #}
    <script src="{{ asset('js/utils/ventanaEmergente.js') }}"></script>
{% endblock %}